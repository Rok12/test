interface BackTabProps {
  furnitureType: string
}

export const BackTab = ({ furnitureType }: BackTabProps) => {
  return <div className="text-center text-gray-500 py-4">Back panel options will be available in a future update.</div>
}
