interface HardwareTabProps {
  furnitureType: string
}

export const HardwareTab = ({ furnitureType }: HardwareTabProps) => {
  return <div className="text-center text-gray-500 py-4">Hardware options will be available in a future update.</div>
}
